'use strict'

const handles = require('./index');
const common = require('../lib/common');
const CONST = require('../lib/constant');

handles.user.makeEquip = async (param, user) => {
    
};

handles.user.sellEquip = async (param, user) => {
    
};